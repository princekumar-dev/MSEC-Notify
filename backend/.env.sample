# Server Configuration
PORT=5000
NODE_ENV=production

# MongoDB Atlas Connection String
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.ahujvyh.mongodb.net/msec_parent_connect?retryWrites=true&w=majority

# WhatsApp Message Sending Delays (in milliseconds)
MESSAGE_DELAY_MIN=4000
MESSAGE_DELAY_MAX=6000

# File Upload Limit (10MB in bytes)
MAX_FILE_SIZE=10485760
